export const apiRoutes = {
  me: '/auth/me',
  login: 'auth/login',
  logout: 'auth/logout',
  classes: '/classes',
};

export const pageRoutes = {
  main: '/',
  auth: '/auth',
  appointment: '/appointment/:id',
  schedule: '/schedule',
};
